[project]
name = "fintual-proof-of-work"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faker>=30.8.1",
]

[tool.pyright]
venvPath = ".venv"

[tool.ruff]
line-length = 120
target-version = "py312"
